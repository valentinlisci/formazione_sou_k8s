apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-chart.fullname" . }}
  labels:
    app: {{ include "flask-chart.name" . }}
spec:
  type: NodePort
  selector:
    app: {{ include "flask-chart.name" . }}
  ports:
    - port: 5000
      targetPort: 5000
      nodeport: 31388
